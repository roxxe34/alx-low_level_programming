#include <stdio.h>
/**
 * main - entry point
 *
 * print single digit numbers
 *
 * return 0 if succeful
 */
int main(void)
{
int i;
for (i = 0; i < 10; i++)
{
printf("%d", i);
}
printf("\n");
return (0);
}
